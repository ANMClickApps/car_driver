import 'package:flutter/material.dart';

class ReatingsTab extends StatelessWidget {
  

  @override
  Widget build(BuildContext context) {
    return Center(
     child: Text('Hello Reatings'),
    );
  }
}
